# AGENTS.md — eye-toy
> Single source of truth for all tools (Claude Code, Cursor, Copilot, etc.)

## Project

Web game inspired by the PlayStation EyeToy. Live webcam feed, real-time hand/body/face detection via MediaPipe, 2D Phaser gameplay rendered on top. 100% client-side, no backend.

## Stack

| Layer | Lib | Version |
|-------|-----|---------|
| Detection | `@mediapipe/tasks-vision` | `^0.10.35` |
| 2D rendering | Phaser 3 | `^3.x` |
| Language | TypeScript | `^6.0` (strict) |
| Bundler | Vite | `^8.0` |
| Package manager | pnpm | `9.x` |
| Tests | Playwright | `^1.59` (Chromium GPU) |

## Commands

```bash
pnpm install          # install deps
node copy-wasm.js     # copy MediaPipe .wasm files into public/wasm/ (auto via predev)
pnpm dev              # dev server (runs copy-wasm as predev)
pnpm build            # production build (runs copy-wasm as prebuild)
pnpm test             # playwright --project=chromium-gpu
pnpm format           # prettier --write
```

## Repo structure

```
eye-toy/
├── AGENTS.md                  ← this file, source of truth
├── CLAUDE.md                  ← @AGENTS.md
├── .cursorrules               ← see AGENTS.md
├── .mcp.json                  ← Context7
├── .claude/settings.json      ← Claude Code permissions
├── copy-wasm.js               ← predev/prebuild script
├── src/
│   ├── scenes/                ← Phaser scenes (Boot, Game, UI, Menu...)
│   ├── camera/
│   │   └── HandTracker.ts     ← MediaPipe loop, emits landmarks
│   ├── game.ts                ← Phaser config
│   └── main.ts                ← entry point
├── public/
│   ├── assets/                ← sprites, audio
│   └── wasm/                  ← generated by copy-wasm.js, gitignored
├── tests/
├── index.html
├── vite.config.ts
├── tsconfig.json
└── package.json
```

## Architecture — non-negotiable rules

1. **HandTracker runs independently of the Phaser game loop.**
   Its loop is a standalone `requestAnimationFrame`.

2. **Zero MediaPipe imports inside Phaser scenes.**
   `HandTracker` is the only layer that touches models and the camera.

3. **Landmarks flow exclusively through the EventEmitter.**
   `HandTracker` emits `"landmarks"`, scenes listen — no direct calls.

4. **No SSR, no Node at runtime.** Everything is client-side.

5. **`public/wasm/` is gitignored** — regenerated on every `pnpm dev` / `pnpm build`.

```
HandTracker (standalone rAF)
    │  emit("landmarks", { hands, pose, face })
    ▼
Phaser.Events.EventEmitter
    │  on("landmarks", handler)
    ▼
Phaser scenes (GameScene, etc.)
```

## Context7 — documentation IDs

Always use Context7 before writing Phaser or MediaPipe code.

| Lib | Context7 ID |
|-----|-------------|
| Phaser 3 (full API) | `/websites/phaser_io` |
| MediaPipe Samples | `/google-ai-edge/mediapipe-samples` |
| MediaPipe core | `/google-ai-edge/mediapipe` |

## Available MediaPipe models

| Model | Landmarks | Use case |
|-------|-----------|----------|
| `HandLandmarker` | 21 pts/hand, 2 hands | Hand-based controls |
| `FaceLandmarker` | 478 pts | Expressions, gaze |
| `PoseLandmarker` | 33 body pts | Full-body gestures |
| `HolisticLandmarker` | All-in-one | When all 3 are active simultaneously |

## Critical notes

- `getUserMedia` requires HTTPS or `localhost` — the Vite dev server must use HTTPS when exposed on the network.
- `.wasm` files must be served with `Content-Type: application/wasm` — Vite does this natively.
- MediaPipe loads models asynchronously; always await `landmarker.setOptions(...)` before starting the detection loop.
- Phaser and MediaPipe each have their own loop — never block the main thread inside `HandTracker`.
